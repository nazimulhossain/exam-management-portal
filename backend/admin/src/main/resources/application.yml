spring:
  application:
    name: "Exam Admin"
  profiles:
    active: "prod"
  datasource:
    url: jdbc:mysql://localhost:9001/examadmindb
    username: root
    password: root

  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: update

  main:
    banner-mode: off

  logging:
    level:
      root: warn





